return {
    "dhruvasagar/vim-table-mode",
    cmd = { "TableModeToggle" },
    -- stylua: ignore
    keys = {
        { "<leader>ot", "<cmd>TableModeToggle<cr>", desc = "Toggle Table Mode" },
    },
}
